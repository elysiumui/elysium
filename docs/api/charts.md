# `elysium.charts`

Charts — `LineChart`, `AreaChart` (stacked), `BarChart`, `DonutChart`/`PieChart`, `Sparkline` and `Legend`, plus `nice_ticks` and the `format_money`/`format_pct`/`format_compact` helpers. The equivalent of QtCharts, drawn on the `DisplayList`.

## Auto-rendered details

::: elysium.charts

## See also

- [Charts and dashboards](../guides/charts-and-dashboards.md)
