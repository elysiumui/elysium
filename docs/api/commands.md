# `elysium.commands`

App-level undo/redo and actions — `Command`/`FunctionCommand`/`MacroCommand`, an `UndoStack`, and `Action` (Qt's `QUndoStack`/`QUndoCommand`/`QAction`).

## Auto-rendered details

::: elysium.commands

## See also

- [Commands and undo](../guides/commands-and-undo.md)
