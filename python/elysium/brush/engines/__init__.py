"""Engine modules. Each file in this directory implements one brush
engine and calls ``register_engine`` at import time. Adding a 7th engine
is a single new file here — the public API picks it up automatically
via the explicit imports in ``elysium/brush/__init__.py``."""
